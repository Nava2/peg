#!@BASH@

# Pipe the file/echo content into the new parser, put the output into the .out
# then diff the result and return it.

cd "@CMAKE_CURRENT_LIST_DIR@"

outfile="./@tgt@.out"
reffile="./@tgt@.ref"
exe="${1}"
@file_command@ | ${exe} | @CAT@ > ${outfile}

@DIFF@ ${reffile} ${outfile}
valid=$? # Save the result of @DIFF@

@RM@ -f ${outfile}
cd -

exit ${valid}